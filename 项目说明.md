# EMG 双手手势项目说明

本文依据当前代码和目录编写，供刚接触项目的同学快速定位功能和文件。运行命令、环境要求与验证状态以根目录 `README.md` 为准；本文重点说明“代码在哪里、数据在哪里、各模块如何连接”。

## 1. 项目在做什么

项目用两只 8 通道 EMG（肌电）手环控制网页音乐游戏。每只手分别接收数据、滤波、取时间窗并用模型识别出四类状态：

- `rest`：放松；
- `fist`：握拳；
- `open-palm`：摊掌；
- `pinch`：捏合。

桌面端把左右手识别结果通过本机 SSE 服务推送给网页游戏；网页根据手势、音符和时机完成判定与计分。

## 2. 当前目录结构和路径约定

```text
EMG_two hands/
├── apps/
│   ├── desktop/                         # Python 桌面端工程
│   │   ├── src/emg_live_marker/         # 可安装的 Python 主包
│   │   ├── scripts/                     # 命令行模块的薄包装
│   │   ├── tests/                       # 自动测试
│   │   ├── models/                      # 当前默认模型根（历史产物仍在此）
│   │   ├── reports/                     # 当前默认报告根（历史产物仍在此）
│   │   ├── pyproject.toml               # 依赖、打包和命令入口
│   │   └── README.md                    # 桌面端说明
│   └── web-game/                        # 网页音乐游戏和演示 API
├── data/
│   ├── datasets/subject_01/session_*/   # 训练、评估用的结构化采集数据
│   ├── recordings/<recording_id>/        # 普通录制数据
│   ├── README.md
│   └── migration-manifest.csv            # 数据迁移文件与哈希清单
├── artifacts/                            # 产物布局约定和历史产物索引
│   ├── README.md
│   └── artifact_manifest.csv
├── docs/
│   └── effie-integration.md              # 外部 EffiE 工程的集成说明
├── third_party/                          # 外部工程，例如 EffiE（本地、不提交）
├── README.md                             # 项目运行与验证基线
└── 项目说明.md                           # 本文
```

旧路径 `2/emg_live_marker/` 和 `UI_EMG/` 已不存在。代码、文档、脚本和命令应使用上表中的 `apps/desktop/` 与 `apps/web-game/` 路径。

原始数据、录制数据、模型、报告和日志不提交普通 Git；`data/` 与 `artifacts/` 中被跟踪的是说明和清单。`.gitignore` 不会自动取消已跟踪文件，若发现生成物被错误跟踪，需先核实后再用 `git rm --cached` 处理。

## 3. 两条运行路线

### 主路线：桌面端到网页游戏

```text
左/右手环（或模拟器）
        ↓ 串口数据包
apps/desktop/src/emg_live_marker/device/protocol.py
        ↓
realtime/ring_buffer.py + realtime/stream_processor.py
        ↓
ml/realtime_decoder.py
        ↓
ml/gesture_model.py 或 ml/effie_adapter.py
        ↓
realtime/gesture_server.py（SSE，127.0.0.1:8766/events）
        ↓
apps/web-game/game.js
```

SSE 是浏览器持续接收服务器消息的连接方式。网页优先连接 `http://127.0.0.1:8766/events`，这是桌面端启动后提供的主服务。

### 轻量演示路线：不接桌面端也可测试网页

`apps/web-game/emg_api.py` 提供 `http://127.0.0.1:8765/events`，并能接收手动发送的手势或合成 EMG 数据。它的阈值判断只用于演示、网页联调和 smoke test，不会加载项目训练模型。

## 4. Python 桌面端：`apps/desktop/`

桌面端采用标准 `src/` 布局，安装后的包名为 `emg_live_marker`。应通过可编辑安装或已注册的控制台命令运行，而不是依赖当前目录、`PYTHONPATH` 或 `sys.path.append(...)`。

### 顶层文件和目录

| 路径 | 作用 |
| --- | --- |
| `apps/desktop/pyproject.toml` | 声明 Python >= 3.10、运行/开发依赖、`src` 包发现规则及控制台命令。 |
| `apps/desktop/src/emg_live_marker/` | 实际可导入的桌面端包。 |
| `apps/desktop/scripts/` | 与 `emg_live_marker.cli` 对应的薄包装脚本；日常更推荐模块命令或控制台命令。 |
| `apps/desktop/tests/` | 12 个测试模块，覆盖协议、缓存、实时处理、界面布局、采集、模型、路径和运行命名。 |
| `apps/desktop/models/` | 当前默认的 `artifacts_root/models`，包含尚未迁移的历史模型。 |
| `apps/desktop/reports/` | 当前默认的 `artifacts_root/reports`，包含尚未迁移的历史评估报告。 |

### 主包模块

| 路径 | 作用 |
| --- | --- |
| `__main__.py` | 解析 `--simulate`、`--port`、`--baudrate` 和通用路径参数，创建 Qt 应用和主窗口。无 `--port` 时使用模拟器。 |
| `config.py` | 默认硬件与信号配置：8 通道、EMG 250 Hz、IMU 104 Hz、921600 波特率、60 秒缓存、20–90 Hz 带通和 100 ms RMS 窗口。 |
| `paths.py` | 全项目路径解析入口，保证不依赖运行时工作目录。 |
| `run_naming.py` | 为新训练运行生成统一的 run ID。 |
| `device/` | 二进制协议解析、真实串口源与模拟手环数据源。 |
| `realtime/` | 环形缓存、连续滤波、普通录制、训练数据采集及 8766 SSE 服务。 |
| `ml/` | 常规 CNN/TCN、EffiE 风格模型、预处理、实时解码和游戏桥接。 |
| `ui/` | Qt 主窗口、多通道波形图和界面样式。 |
| `integrations/effie.py` | 外部 EffiE 相关的集成辅助代码。 |

### 关键模块的职责

- `device/protocol.py`：解析 29 字节数据包，处理 `D2 D2 D2` 包头、`AA` EMG 包、`BB` IMU 包、半包、粘包与丢包统计。
- `device/serial_source.py`：在 Qt 线程中读取真实串口；`device/simulator.py`：在无硬件时产生模拟 EMG 与 IMU 数据。
- `realtime/ring_buffer.py`：为每只手保存最近一段数据，按样本序号排序去重。
- `realtime/stream_processor.py`：连续带通、可选工频陷波、整流和滑动 RMS，保持滤波器跨数据块状态。
- `realtime/recorder.py`：普通录制到 `data/recordings/<timestamp>/`。
- `realtime/collection.py`：组织 `rest → gesture → rest` 的训练采集试次，写到 `data/datasets/<subject>/<session>/`。
- `realtime/gesture_server.py`：对网页发布 SSE，端口为 8766。
- `ml/realtime_decoder.py`：周期性取最近数据窗，在后台推理，并以置信度、投票与切换确认降低手势抖动。
- `ml/gesture_model.py`：常规 CNN/TCN 手势分类模型与 `.pt` / TorchScript `.ts` 加载器。
- `ml/effie_adapter.py` 与 `ml/effie_preprocess.py`：EffiE 风格 PyTorch 模型和预处理；输入由 250 Hz 重采样到 200 Hz，使用 8×32 时间窗。
- `ui/main_window.py`：将双手的串口/模拟器、缓存、波形、录制、采集、模型选择和网页 SSE 串联起来。

### 默认模型选择

当前主窗口会在模型根目录（默认 `apps/desktop/models/`）依次寻找：

```text
effie_real_full_v2_continue/gesture_classifier.ts
calibration_game_model/gesture_classifier.ts
gesture_classifier.pt
```

这是历史兼容行为，不表示这些模型已被审核为最终版本。模型、`gesture_labels.json`、`normalization.json` 与 `model_info.json` 必须来自同一次训练，不能随意混用。

## 5. 网页游戏：`apps/web-game/`

| 路径 | 作用 |
| --- | --- |
| `index.html` | 页面结构：标题、按钮、左右手状态、分数面板和 canvas 画布。 |
| `styles.css` | 页面布局、颜色、按钮与响应式样式。 |
| `game.js` | 《小星星》谱面、手势 SSE 连接、音符判定、得分、音频和 canvas 动画。 |
| `emg_api.py` | 8765 演示 HTTP/SSE 服务，含 `/health`、`/events`、`/gesture`、`/emg`。 |
| `send_demo_gesture.py` | 向 8765 服务发送指定手势的命令行工具。 |
| `white_records-*.mp3` | 网页音频资源，不是程序代码。 |

`game.js` 会优先连接桌面端 8766；连接不可用时才回退到 8765 演示服务。无手环时也可使用键盘映射测试网页判定。

## 6. 数据、模型和报告路径

`apps/desktop/src/emg_live_marker/paths.py` 的路径优先级为：

```text
命令行参数 > .emg-paths.json 配置文件 > 环境变量 > 仓库默认路径
```

| 变量/参数 | 默认位置 | 用途 |
| --- | --- | --- |
| `--dataset-root` / `EMG_DATASET_ROOT` | `data/datasets` | 结构化训练、评估会话。 |
| `--recordings-root` / `EMG_RECORDINGS_ROOT` | `data/recordings` | 普通录制会话。 |
| `--artifacts-root` / `EMG_ARTIFACTS_ROOT` | `apps/desktop` | 其下的 `models/` 和 `reports/`。 |
| `--paths-config` / `EMG_PATHS_CONFIG` | 根目录 `.emg-paths.json` | 指定本机 JSON 路径配置文件。 |

如果希望新训练从现在起写入目标产物布局，可传入 `--artifacts-root artifacts`。根目录 `artifacts/` 的预期结构为：

```text
artifacts/
├── models/<model>/<split>/<mode>/<run-id>/
├── reports/
├── logs/
├── duplicate-review/
└── legacy-unclassified/
```

历史模型与报告尚未实际迁移；`artifacts/artifact_manifest.csv` 的 `new_path` 是建议目标而非已移动证明。涉及 `smoke`、`debug`、`now` 或疑似重复文件时，先根据清单审阅，不要直接删除。

### 数据结构

```text
data/
├── datasets/subject_01/session_001/  # 其余当前会话同级排列
│   ├── metadata.json
│   ├── emg.csv
│   ├── imu.csv
│   ├── events.csv
│   └── raw_packets.bin
└── recordings/<recording_id>/         # 同样五类采集文件
```

当前数据集有 `session_001`、`002`、`003`、`004`、`005`、`009`、`011`、`012`、`013`。`session_003` 和 `session_004` 的异常同名嵌套已在迁移时扁平化；原始路径、目标路径和 SHA-256 记录在 `data/migration-manifest.csv`。录制数据中存在三组内容相同的候选重复目录，已全部保留并在清单中标记，尚未删除。

## 7. 训练、评估和检查命令

在仓库根目录使用 BN5213 环境：

```bash
PYTHON=/Applications/anaconda3/envs/BN5213/bin/python
$PYTHON -m pip install --no-deps -e apps/desktop

# 检查数据集健康状况
$PYTHON -m emg_live_marker.cli.inspect_gesture_dataset \
  --dataset-root data/datasets

# 常规分类器训练
$PYTHON -m emg_live_marker.cli.train_gesture_classifier \
  --dataset-root data/datasets \
  --device auto

# 离线评估实时平滑效果
$PYTHON -m emg_live_marker.cli.evaluate_realtime_smoothing \
  --dataset-root data/datasets \
  --model-path apps/desktop/models/<run_id>/gesture_classifier.ts \
  --output-dir apps/desktop/reports/<run_id> \
  --session session_001
```

未给 `--output-dir` 时，训练和 EffiE 微调按以下规则生成目录名：

```text
{model}__{split}__{mode}__{timestamp}__seed-{seed}
```

外部 EffiE 项目应放在 `third_party/EffiE/`；相关命令与限制参见 `docs/effie-integration.md`。该外部项目与 checkpoint 不在仓库中，缺失时不能自行假定其存在。

## 8. 测试和已知验证状态

测试位于 `apps/desktop/tests/`。其中新增的 `test_paths.py` 覆盖路径优先级，`test_run_naming.py` 覆盖运行命名；其余测试覆盖协议、缓存、连续处理、波形布局、采集、模型、平滑评估和主窗口布局。

当前可用的全量测试命令为：

```bash
QT_QPA_PLATFORM=offscreen PYTHONDONTWRITEBYTECODE=1 \
  $PYTHON -m pytest -q apps/desktop/tests
```

在 BN5213 / Python 3.10.19 中，已确认可编辑安装、无显示器条件下的桌面主窗口初始化、路径/运行命名/训练产物的针对性测试（6 passed），以及从仓库外发现 9 个数据集会话。完整测试套件尚未作为一次全量、无告警验收重新运行完毕，因此不能把它表述为全绿。

## 9. 修改时的边界和常见问题

1. 双手拥有独立缓存与解码器；不要混写左、右手数据。
2. 训练期的采样率、窗口长度、标签顺序和归一化参数必须与实时推理保持一致。
3. `events.csv` 是训练标签来源；仅有 `emg.csv` 不足以可靠训练。
4. 网页优先监听 8766，8765 仅为演示回退服务；两条路线不要混淆。
5. `data/`、`artifacts/`、`third_party/` 中的大文件默认不进 Git。测试样本应放入 `apps/desktop/tests/fixtures/`，且只放可公开、最小化的数据。
6. 若环境中缺少项目依赖，先报告缺失项并确认后再安装；本项目标准环境是 BN5213，不再保留项目内 `venv/`。

## 10. 按目标查找文件

| 目标 | 首先查看 |
| --- | --- |
| 启动或连接桌面端 | `apps/desktop/src/emg_live_marker/__main__.py`、`ui/main_window.py` |
| 修改串口协议或模拟器 | `apps/desktop/src/emg_live_marker/device/` |
| 修改滤波和实时处理 | `apps/desktop/src/emg_live_marker/realtime/stream_processor.py` |
| 录制或采集训练数据 | `apps/desktop/src/emg_live_marker/realtime/recorder.py`、`collection.py` |
| 训练常规模型 | `apps/desktop/src/emg_live_marker/cli/train_gesture_classifier.py` |
| 微调/导入 EffiE | `apps/desktop/src/emg_live_marker/cli/finetune_effie_gesture.py`、`import_effie.py` |
| 调整实时稳定性 | `apps/desktop/src/emg_live_marker/ml/realtime_decoder.py`、`cli/evaluate_realtime_smoothing.py` |
| 修改游戏规则和页面 | `apps/web-game/game.js`、`index.html`、`styles.css` |
| 验证网页可接收手势 | `apps/web-game/emg_api.py`、`send_demo_gesture.py` |

如果开始一次新的改造，请先在模拟数据下验证桌面端和网页连接，再接入单只手环，最后才进行双手采集和训练。这样能把硬件、实时通信和模型问题分开定位。
