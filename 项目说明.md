# EMG 双手手势项目：说明书

> 本文按当前文件夹中的实际内容编写，目标是帮助第一次接触 Python、网页和机器学习的同学看懂项目。这里的“代码文件”指可以阅读和修改的 `.py`、`.js`、`.html`、`.css`、`.toml` 文件；`.pt`、`.ts`、`.csv` 等是模型或数据，不是需要逐行阅读的程序。

## 1. 这个项目总体在做什么？

这是一个用两只 **8 通道肌电（EMG）手环**控制网页音乐游戏的项目。

肌电可以简单理解为：肌肉收缩时产生的微弱电信号。程序把手环送来的 8 路数值变成波形，经过滤波与机器学习模型，识别为四类状态：

- `rest`：放松；
- `fist`：握拳；
- `open-palm`：摊掌；
- `pinch`：捏合。

左、右手各自独立识别。识别结果会通过本机网络发送给网页游戏，网页再按照音符、手势和时机计算得分。

## 2. 最重要的运行路线

项目有两套可以连接游戏的服务：

1. **主要路线（现在桌面程序实际使用）**：运行 Python 桌面端 `emg_live_marker`。它自己启动 `http://127.0.0.1:8766/events`，网页直接从这里接收左右手的识别结果。
2. **轻量演示路线**：运行 `UI_EMG/emg_api.py`。它启动 `http://127.0.0.1:8765`，可用命令手动发送一个手势给网页。这个接口内置的是演示判断，不是真正接入训练模型的完整实现。

核心数据流如下：

```text
左/右 EMG 手环（或模拟器）
          ↓ 串口数据包
device/protocol.py 解析二进制包
          ↓
realtime/ring_buffer.py 缓存最近的波形
          ↓
realtime/stream_processor.py 滤波、整流、RMS
          ↓
ml/realtime_decoder.py 取一个时间窗进行识别
          ↓
ml/gesture_model.py 或 ml/effie_adapter.py 得到手势
          ↓
realtime/gesture_server.py（8766 的 SSE 推送）
          ↓
UI_EMG/game.js 接收左右手手势、判定音符、绘制和计分
```

这里的 **SSE** 是“服务器主动持续推送消息给网页”的轻量通信方式；浏览器不用不断询问“有新手势吗？”。

## 3. 先从哪些文件看起？

建议按下面顺序阅读，不必一开始就看训练脚本和深度学习网络。

1. `2/emg_live_marker/README.md`：安装、运行、手环通信格式的官方说明。
2. `2/emg_live_marker/emg_live_marker/__main__.py`：桌面程序的启动入口。
3. `2/emg_live_marker/emg_live_marker/ui/main_window.py`：桌面窗口如何把所有功能组装起来。
4. `2/emg_live_marker/emg_live_marker/device/protocol.py`：手环发来的字节如何还原成 EMG/IMU 数值。
5. `2/emg_live_marker/emg_live_marker/ml/realtime_decoder.py`：如何定时调用模型得到稳定手势。
6. `UI_EMG/index.html`、`UI_EMG/game.js`、`UI_EMG/styles.css`：网页游戏的结构、逻辑、外观。

## 4. 根目录每个文件夹的用途

```text
EMG_two hands/
├─ 2/                 Python 桌面端工程所在的外层目录；名字只是容器名，没有技术含义
├─ UI_EMG/            浏览器音乐游戏及其简易手势 API
├─ .agents/           开发工具/智能体的本地配置，不参与运行
├─ .idea/             JetBrains/PyCharm 的本地工程配置，不参与运行
└─ .git/              Git 版本管理的内部记录，不要手动修改
```

### `.agents/`、`.idea/`、`.git/`

它们都不是业务代码：

- `.agents/`：开发辅助工具保存的工作信息；
- `.idea/`：PyCharm/IntelliJ 的窗口、解释器、索引等个人设置；
- `.git/`：Git 保存提交历史、分支和改动记录的位置。

一般不需要修改，也不应因为“看起来没用”就删除。

## 5. `UI_EMG/`：网页双手音乐游戏

这个目录可以理解为“游戏前端 + 一个方便调试的本地接口”。

| 文件 | 是什么 | 作用 |
| --- | --- | --- |
| `index.html` | 网页骨架 | 放置标题、开始按钮、说明、左右手状态、分数面板和游戏画布（`canvas`）。浏览器先读它。 |
| `styles.css` | 网页样式表 | 设置页面布局、字体、颜色、按钮、左右手卡片和响应式尺寸；不负责游戏规则。 |
| `game.js` | 网页游戏逻辑 | 生成《小星星》音符、播放音色、绘制动画、接收 SSE 手势、判定 Perfect/Good/Miss、维护左右手得分。 |
| `emg_api.py` | 简易 HTTP/SSE 服务 | 接收 `/gesture` 或 `/emg` 请求，把结果广播到网页；主要用于演示和联调。 |
| `send_demo_gesture.py` | 命令行演示工具 | 向 `emg_api.py` 发送一个指定手势，便于不接手环时测试网页。 |
| `white_records-...mp3` | 音频资源 | 可作为游戏背景音乐/音效资源；不是代码。 |
| `.agents/`、`.git/`、`__pycache__/` | 工具或缓存目录 | 分别是开发辅助信息、版本记录、Python 自动生成的字节码缓存；不属于游戏功能代码。 |

### `UI_EMG/index.html`

HTML 是网页的“结构说明书”。此文件定义了：

- 游戏标题与使用提示；
- 开始/重开等按钮；
- 左右手当前识别手势与连接状态；
- 分数、连击等文字区域；
- `canvas` 画布，真正下落的音符和特效由 JavaScript 画在这里；
- 对 `styles.css` 与 `game.js` 的引用。

它本身几乎不做判断。想调整文字、增加一个按钮或面板时看这里。

### `UI_EMG/styles.css`

CSS 是网页的“装修图纸”。它控制：背景、卡片边框、按钮状态、左右轨道的视觉区分、适配小屏幕时的布局等。想改颜色、字体大小、按钮圆角、页面间距时改此文件；不要在 CSS 里写手势识别算法。

### `UI_EMG/game.js`

这是网页端最重要的代码文件，分成以下几块：

- **常量与翻译表**：规定允许的手势、左右手、颜色、中文显示文字，以及音高对应哪种手势。
- **曲谱数据**：`TWINKLE_MELODY` 存放《小星星》的音高和节拍；`generateChart()` 把它变成待判定的音符。
- **`state`**：集中保存游戏是否开始、当前时间、分数、连击、左右手识别结果、音符和特效等运行状态。
- **实时手势连接**：`connectRealtimeGestureApi()` 首先尝试桌面端的 `8766/events`，失败后再尝试简易 API 的 `8765/events`；收到 SSE 后调用 `handleGesture()`。
- **判定与得分**：`handleGesture()` 判断该手是否在正确时间做了正确手势；`applyGrade()` 处理 Perfect/Good；`missNote()` 处理漏掉或错误的音符。
- **音频**：`ensureAudio()` 创建浏览器音频环境，`playTone()` 用 Web Audio API 播出音高。
- **绘制与动画**：`drawBackground()`、`drawTrack()`、`drawNote()`、`drawEffects()` 等函数在 canvas 上画背景、轨道、音符、粒子和结束画面。
- **主循环**：`loop()` 每一帧更新状态和重新绘制；`startGame()`、`endGame()` 负责开局与结算。
- **键盘测试**：文件尾部的键位映射可在没有手环时用键盘模拟手势。

想改变“哪个手势代表哪个音高”，看 `pitchToGesture()`；想改判定宽松程度，找 `HINT_LEAD` 和与时间差有关的判定代码；想改乐曲，修改 `TWINKLE_MELODY`。

### `UI_EMG/emg_api.py`

这是一个只用 Python 标准库写的小服务器，默认监听 `127.0.0.1:8765`。

- `GestureBroadcaster`：保存已连接网页，并把同一条手势消息发给所有网页。
- `GestureClassifier`：预留给真正模型的接口；目前未传模型时用信号能量阈值生成**演示结果**，传入模型路径也尚未真正加载模型。因此它不是项目训练模型的主推理通道。
- `normalize_gesture()`、`normalize_hand()`：把 `open`、`palm`、`l`、`r` 等别名统一成项目使用的标准写法。
- `EmgRequestHandler`：处理 HTTP 请求。
  - `GET /health`：检查服务是否还活着；
  - `GET /events`：建立 SSE 长连接，供网页接收手势；
  - `POST /gesture`：直接发送 `gesture`、`confidence`、可选 `hand`；
  - `POST /emg`：接收一段 `samples`，交给 `GestureClassifier` 决定手势。
- `run()`：创建服务器并一直运行。

### `UI_EMG/send_demo_gesture.py`

这个脚本是最容易理解的测试工具。运行前先启动 `emg_api.py`，然后例如：

```powershell
python send_demo_gesture.py fist 0.93 --hand left
```

它会向 `http://127.0.0.1:8765/gesture` 发送 JSON。`fist` 是手势，`0.93` 是置信度，`--hand left` 表示左手。

## 6. `2/emg_live_marker/`：Python 桌面端主工程

`2` 这个目录名不是 Python 术语；真正的工程根目录是 `2/emg_live_marker/`。它包含桌面 GUI、串口采集、录制、训练、推理、模型和测试。

```text
2/emg_live_marker/
├─ emg_live_marker/        可安装的主 Python 包（运行时核心）
├─ scripts/                离线训练、分析、比较脚本
├─ tests/                  自动测试
├─ models/                 已训练模型与训练结果
├─ recordings/             普通录制会话
├─ reports/                模型平滑效果评估报告
├─ external_models/effie/  外部 EffiE 模型的适配说明/转换脚本
├─ emg_live_marker.egg-info/ 安装后生成的包信息
├─ venv/                   本机 Python 虚拟环境（若存在）
├─ pyproject.toml          项目依赖和打包配置
└─ README.md               原始运行说明
```

### 顶层文件

| 文件 | 用途 |
| --- | --- |
| `README.md` | 原始英文说明：安装、启动模拟器/串口、校准训练、EffiE 微调和手环包格式。 |
| `pyproject.toml` | Python 项目的“说明和依赖清单”。规定 Python 需至少 3.10，并安装 PySide6、pyqtgraph、pyserial、numpy、scipy、torch；`[dev]` 额外安装 pytest 和 ruff。还注册了 `emg-live-marker` 命令。 |

### `emg_live_marker/` 主包

#### 根级文件

| 文件 | 用途 |
| --- | --- |
| `__init__.py` | 将文件夹标记为可导入的 Python 包；当前几乎没有业务逻辑。 |
| `__main__.py` | 程序入口。解析 `--simulate`、`--port COM4`、`--baudrate 921600`，创建 Qt 应用与主窗口。没有给 `--port` 时默认启动模拟器。 |
| `config.py` | 集中放默认参数：8 通道、EMG 250Hz、IMU 104Hz、60 秒缓存、20–90Hz 带通、100ms RMS 窗口等。`AppConfig` 是这些参数的数据结构。 |

#### `device/`：和手环通信

| 文件 | 用途 |
| --- | --- |
| `__init__.py` | 包标记文件。 |
| `protocol.py` | 最底层通信协议。定义 29 字节包、包头 `D2 D2 D2`、`AA`（EMG）和 `BB`（IMU）；把 24 位/16 位有符号整数还原为数值；`PacketParser` 能处理半包、粘包、杂字节和丢包统计。`build_aa_packet()`、`build_bb_packet()` 主要供模拟器与测试造包。 |
| `serial_source.py` | 真手环入口。`SerialSource` 在独立 Qt 线程中打开 COM 口、读取字节、调用 `PacketParser`，并用 Qt 信号把 EMG、IMU、原始字节和统计信息送回界面；`list_serial_ports()` 用于下拉框列出 COM 口。 |
| `simulator.py` | 没有硬件时的假手环。定时生成带噪声的 8 通道 EMG 和 6 轴 IMU 数据，并偶尔制造肌肉活动突发，方便检查界面和数据管线。 |

#### `realtime/`：实时数据处理、录制和数据采集

| 文件 | 用途 |
| --- | --- |
| `__init__.py` | 包标记文件。 |
| `ring_buffer.py` | 固定容量的“循环缓存”。`EmgRingBuffer` 和 `ImuRingBuffer` 只保留最近一段时间的数据，避免无限占内存；还会按样本编号排序、去重，提供按秒取窗口的功能。 |
| `processing.py` | 较简单的 EMG 批处理工具：根据显示模式得到原始、整流或 RMS 信号。适合小段数据的处理与测试。 |
| `stream_processor.py` | 真正面向连续流的滤波器。`StreamingEMGProcessor` 对每一小块数据持续做带通、可选工频陷波（50/60Hz 及谐波）、整流、滑动 RMS，且维护滤波器状态，避免块与块之间断裂。 |
| `recorder.py` | 普通录制器。开始录制后建立带时间戳的文件夹，持续写入元数据、EMG、IMU、事件和原始字节。 |
| `collection.py` | 有提示的训练数据采集逻辑。建立 `rest → 手势保持 → rest` 的试次列表，定义收集时支持的三种动作及其显示名。 |
| `gesture_server.py` | 桌面端给网页使用的 SSE 服务，默认端口 `8766`。每个网页客户端有独立消息队列；提供 `/health` 和 `/events`，主窗口可把左右手 `gesture`、`hand_status` 推送过去。 |

#### `ml/`：机器学习识别和游戏桥接

| 文件 | 用途 |
| --- | --- |
| `__init__.py` | 包标记文件。 |
| `preprocess.py` | 常规模型的输入预处理：清洗非数值、截取/补齐固定长度时间窗、按训练期保存的均值和标准差归一化，再转为 PyTorch 张量。 |
| `gesture_model.py` | 常规手势模型定义和加载器。包含较小的 CNN `EMGGestureNet`、时间卷积网络 `EMG2PoseGestureTCN`、统一预测包装器 `GesturePredictor`，以及没有模型时用于演示的 `DemoGesturePredictor`。能读取 `.pt` 或 TorchScript `.ts` 以及同目录元数据。 |
| `effie_preprocess.py` | EffiE 风格模型的特殊预处理：把 250Hz 的原始 EMG 重采样到 200Hz，取 8×32 窗口，并执行归一化。 |
| `effie_adapter.py` | 用 PyTorch 实现的 EffiE 风格 2D CNN、模型加载和预测包装器。它可选择冻结特征提取骨干或全量微调；不是把外部 TensorFlow 项目直接塞进主程序。 |
| `emg2pose_adapter.py` | 为 emg2pose 相关模型/格式准备的适配层，使不同模型可通过相近的预测接口使用。 |
| `realtime_decoder.py` | 实时推理调度器。周期性从环形缓存取最近窗口，在后台线程推理，按置信度阈值、历史投票、状态切换确认和 pinch 门限让输出更稳定，再向界面发出结果。 |
| `game_bridge.py` | 老的 HTTP 桥接方式：把预测结果异步 POST 到 `8765/gesture`。会限制发送频率，低置信度强制视为 `rest`，避免网页被过多请求堵塞。 |
| `dual_game_mapper.py` | 键盘映射备用方案：可把左/右手势映射成 A、D、W、Space 的按住/松开事件。当前主窗口用 `NoopKeySink` 关闭了真实键盘注入，优先使用 SSE 控制网页，避免 Space 误触开始游戏。 |

#### `ui/`：桌面界面

| 文件 | 用途 |
| --- | --- |
| `__init__.py` | 包标记文件。 |
| `main_window.py` | 最大、最核心的组装文件。`MainWindow` 创建左右手各自的缓存、滤波器、解码器和连接；搭建串口/模拟器、波形、录制、采集、模型加载、游戏控制等控件；启动 `GestureServer(8766)`；并把识别结果、录制数据、波形刷新和采集流程串起来。`BraceletRuntime` 是“某一只手运行状态”的数据盒子。 |
| `waveform_view.py` | 8 通道波形图组件。`sanitize_plot_data()` 清洗 NaN、无穷大、异常点和样本缺口，`MultiChannelWaveformView` 使用 pyqtgraph 绘制等宽多通道曲线及事件标记。 |
| `style.py` | `APP_QSS`：Qt 界面的样式表，负责亮色主题、控件颜色、边框与间距。 |

#### `assets/`

| 文件 | 用途 |
| --- | --- |
| `logo.jpg` | 桌面软件可使用的图片资源。 |

## 7. `scripts/`：不是日常启动必需，而是训练/分析工具

这些脚本应在 `2/emg_live_marker/` 目录下运行。它们会读取收集的数据，生成或比较 `models/` 和 `reports/` 中的结果。

| 文件 | 用途 |
| --- | --- |
| `__init__.py` | 让脚本目录可作为 Python 包导入。 |
| `train_gesture_classifier.py` | 训练常规 CNN/TCN 分类器的主脚本。读取 EMG 和事件标注、按试次切时间窗、处理类别不平衡、划分训练/验证集、训练、计算混淆矩阵，并保存权重和元数据；也支持跨会话验证。 |
| `finetune_effie_gesture.py` | 微调 EffiE 风格模型。读取相同数据集，构建 EffiE 输入，可以冻结骨干网络或全量微调，输出模型、标准化参数、训练报告和 TorchScript。 |
| `inspect_gesture_dataset.py` | 数据体检工具。检查每个会话 EMG/事件文件是否齐全、样本数和动作分布是否合理，帮助在训练前发现录制问题。 |
| `compare_gesture_models.py` | 读取多个候选模型目录的元数据/报告，汇总比较，方便选择要在游戏里加载的模型。 |
| `evaluate_realtime_smoothing.py` | 离线模拟实时识别并比较平滑参数。输出逐窗口日志、准确率、切换次数、检测延迟和参数扫描摘要，帮助在“稳定”和“反应快”之间取平衡。 |

## 8. `tests/`：自动检查代码是否被改坏

测试文件不直接给用户提供功能，但每个都覆盖了一块重要逻辑。运行 `pytest` 时会自动执行它们。

| 文件 | 检查的内容 |
| --- | --- |
| `test_protocol.py` | 24/16 位数值转换、AA/BB 包解析、半包/粘包、重新同步、丢包统计和造包函数。 |
| `test_ring_buffer.py` | 环形缓存容量、时间顺序、样本编号、去重和空数据处理。 |
| `test_processing.py` | 原始/整流/RMS 显示模式的形状与数值合理性。 |
| `test_stream_processor.py` | 连续带通/陷波/RMS 处理是否稳定，是否支持 50/60Hz 及谐波。 |
| `test_waveform_sanitize.py` | 波形显示前能否剔除 NaN、无穷大、异常值，能否在采样缺口处断线。 |
| `test_waveform_layout.py` | 多通道波形图的坐标轴宽度和时间轴布局。 |
| `test_collection_mode.py` | 训练数据采集试次、事件阶段、暂停恢复、时间戳和写盘格式。 |
| `test_ml_game_decoder.py` | 预处理、模型形状、模型加载、双手独立解码、置信度与 pinch 门限、游戏桥接、训练脚本的小型端到端测试。 |
| `test_realtime_smoothing_eval.py` | 平滑评估脚本是否会正确生成日志、摘要和参数扫描表。 |
| `test_main_window_layout.py` | 主窗口的控件布局、波形刷新、左右手缓存独立性、模型选择、采集与游戏控制是否符合预期。 |

## 9. 数据文件夹：录制数据从哪里来？

### 普通录制：`recordings/`

当前有 `2026-07-10_14-14-13`、`2026-07-10_14-14-28`、`2026-07-10_14-17-57` 三个普通录制会话。每个会话目录结构相同：

| 文件 | 内容 |
| --- | --- |
| `metadata.json` | 本次录制的时间、采样率、通道数和设置等说明。 |
| `emg.csv` | 8 路肌电逐样本数据；通常是训练和检查的主要信号文件。 |
| `imu.csv` | 陀螺仪和加速度计数据。 |
| `events.csv` | 用户标记、采集阶段等事件及其时间/样本位置。 |
| `raw_packets.bin` | 从设备收到的原始二进制字节；用于协议排错或重新解析。 |

### 训练集：`emg_live_marker/dataset/subject_01/`

这是按“受试者 → 会话”组织的数据集。当前包含 `session_001`、`002`、`003`、`004`、`005`、`009`、`011`、`012`、`013`。每个会话也有同样五类文件：`metadata.json`、`emg.csv`、`imu.csv`、`events.csv`、`raw_packets.bin`。

其中 `session_003/session_003/` 和 `session_004/session_004/` 多了一层同名目录；这是以前复制/导出时留下的目录结构差异，训练脚本会尝试发现会话目录，但整理数据时最好统一为一层会话目录。

### 包内旧录制：`emg_live_marker/recordings/`

这里有 `2026-06-29_12-32-36` 与 `2026-07-08_12-02-44` 两个同格式会话。它们位于 Python 包内，不是最理想的数据存放位置；功能上仍是旧的录制样本。

## 10. `models/`：训练结果，不是源码

模型目录下的名字表示不同训练实验。它们可分成四类：

| 目录/文件 | 含义 |
| --- | --- |
| `gesture_classifier.pt`、`best_gesture_classifier.pt`、`2026-07-08_emg2pose_1_gesture_classifier.pt`、`best_pretrained_lr_sweep_gesture_classifier(2).pt` | 单独放在根目录的 PyTorch 权重文件。 |
| `emg2pose_gesture_v1`、`emg2pose_gesture_cv` | 使用 emg2pose 思路的时间卷积模型；`cv` 是交叉验证，里面按 `fold_session_001` 等留出会话分折。 |
| `effie_*` | EffiE 风格模型的烟雾测试、冻结骨干、全量微调、继续训练、跨会话验证等实验版本。 |
| `smoke_*`、`debug_trial_split`、`now` | 快速验证、调试或临时实验输出，通常不应直接当作最终演示模型。 |

各完整模型实验目录通常重复以下文件：

| 文件 | 用途 |
| --- | --- |
| `gesture_classifier.pt` | PyTorch 原始权重，主要供 Python 加载。 |
| `gesture_classifier.ts` | TorchScript 导出模型，便于部署；主窗口会优先加载它。 |
| `gesture_labels.json` | 类别名与顺序，例如 `rest/fist/open-palm/pinch`。类别顺序必须与模型输出对应。 |
| `normalization.json` | 训练期的均值和标准差；推理必须用同一套数值，否则准确率会显著下降。 |
| `model_info.json` | 模型类型、窗口长度、采样率、输入信号类型等运行所需说明。 |
| `train_report.json` | 训练参数、训练/验证指标等报告。 |
| `confusion_matrix.csv` | 混淆矩阵：真实动作和预测动作分别落在何处，用于观察哪些手势容易混淆。 |
| `cv_report.json` | 仅交叉验证总目录常见；汇总各折的效果。 |

当前桌面端默认优先选择：

```text
models/effie_real_full_v2_continue/gesture_classifier.ts
```

若它不存在，则尝试当天校准模型 `models/calibration_game_model/gesture_classifier.ts`，最后才回退到 `models/gesture_classifier.pt`。这段优先级在 `ui/main_window.py` 的 `default_game_model_path()` 中。

## 11. `reports/`：平滑参数评估结果

`reports/` 下现有 `smoothing_raw`、`smoothing_raw_session001`、`effie_smoothing_raw_session001`、`effie_smoothing_light_session001`、`effie_smoothing_smooth_session001`、`effie_smoothing_sweep_session001` 等实验目录。

- `realtime_smoothing_log.csv`：逐时间窗记录原始预测、平滑后预测、真实标签、概率等；
- `summary.json`：一次评估的指标摘要；
- `sweep_summary.csv`：仅参数扫描实验有，用来比较多组平滑参数。

它们回答的是“输出会不会来回跳”“动作被识别得慢不慢”，而不是保存可直接运行的模型。

## 12. `external_models/effie/`：外部模型衔接

| 文件 | 用途 |
| --- | --- |
| `README.md` | 说明怎样克隆公开 EffiE 项目、准备 checkpoint、运行微调脚本。外部完整仓库本身没有被复制进当前项目。 |
| `import_effie.py` | 把外部 TensorFlow/Keras 风格的 checkpoint 转为本项目可在 PyTorch 侧使用的中间形式的辅助脚本。 |
| `effie_adapter.py` | 外部模型转换/适配时使用的辅助代码；与主包中的 `ml/effie_adapter.py` 配合。 |

## 13. 自动生成目录，不要当作手写业务代码

| 目录 | 为什么出现 | 建议 |
| --- | --- | --- |
| `emg_live_marker.egg-info/` | `pip install -e .` 或打包时生成，含包名、依赖、入口点、文件清单。 | 通常不手改；重新安装可再生成。 |
| `venv/` | 本机虚拟环境，里面装 Python 解释器和依赖。 | 不提交、不逐个阅读；换电脑后按 README 重建。 |
| `__pycache__/` | Python 运行时生成的加速缓存 `.pyc`。 | 可以删除，运行后会自动再生成。 |

`emg_live_marker.egg-info/` 内的文件含义：`PKG-INFO` 是包元数据，`requires.txt` 是依赖，`entry_points.txt` 是命令入口，`SOURCES.txt` 是打包文件清单，`top_level.txt` 是顶层包名，`dependency_links.txt` 是旧式依赖链接信息。

## 14. 新手最常用的命令

在 PowerShell 中先进入桌面端工程：

```powershell
cd "E:\code\EMG_two hands\2\emg_live_marker"
```

第一次安装：

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -e ".[dev]"
```

不接硬件，启动模拟数据桌面端：

```powershell
python -m emg_live_marker --simulate
```

接真实左手手环（例：COM4）：

```powershell
python -m emg_live_marker --port COM4 --baudrate 921600
```

运行自动测试：

```powershell
pytest
```

启动轻量网页接口并手动发手势（另开两个终端，在 `UI_EMG` 内运行）：

```powershell
python emg_api.py
python send_demo_gesture.py fist 0.93 --hand left
```

## 15. 修改时最容易踩的坑

1. **模型、标签、归一化参数必须成套使用。** 不要只换 `.pt`/`.ts`，却继续使用另一个目录的 `gesture_labels.json` 或 `normalization.json`。
2. **两只手是两套独立缓存和解码器。** 不要把左手数据写进右手缓存，否则网页虽然会动，动作会完全错位。
3. **EMG 采样率和窗口长度不可随意改。** 当前硬件数据约为 250Hz；模型训练时看到的窗口形状必须和实时推理一致。
4. **游戏优先监听 8766。** 若要使用 `UI_EMG/emg_api.py` 的 8765 服务，应确保桌面端 8766 服务未占用或网页已回退到 8765；两套服务不要混淆。
5. **训练数据中 `events.csv` 很关键。** 它告诉训练脚本“哪一段是真正握拳/摊掌/捏合”；只有 `emg.csv` 而没有正确事件标记，训练标签就会错。
6. **部分旧中文注释显示为乱码。** 这是源文件历史编码问题，通常不影响 Python 运行；要编辑这些注释时建议统一保存为 UTF-8。

## 16. 一句话分工表

| 你想做的事 | 首先看哪里 |
| --- | --- |
| 启动桌面采集软件 | `emg_live_marker/__main__.py`、`README.md` |
| 看手环波形或连 COM 口 | `device/`、`ui/main_window.py` |
| 修改滤波/工频干扰处理 | `realtime/stream_processor.py` |
| 录制训练数据 | `realtime/collection.py`、`realtime/recorder.py` |
| 训练自己的动作模型 | `scripts/train_gesture_classifier.py` |
| 微调 EffiE 模型 | `scripts/finetune_effie_gesture.py` |
| 调整识别稳定性 | `ml/realtime_decoder.py`、`scripts/evaluate_realtime_smoothing.py` |
| 改网页游戏规则/歌曲/画面 | `UI_EMG/game.js`、`index.html`、`styles.css` |
| 只测试网页是否能收到手势 | `UI_EMG/emg_api.py`、`send_demo_gesture.py` |

---

如果下一步要真正开始使用，最推荐先运行模拟器，确认桌面端的双手波形和网页连接都正常；然后再接一只手环，最后才做双手采集和重新训练。这样排错最简单。
